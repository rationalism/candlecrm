(ns spectra_cljs.html
  (:require [clojure.string :as str]
            [spectra_cljs.ajax :as ajax]
            [spectra_cljs.calendar :as calendar]
            [spectra_cljs.map :as map]
            [spectra_cljs.regex :as regex]
            [spectra_cljc.schema :as s]
            [spectra_cljs.search :as search]
            [spectra_cljs.state :as state]
            [spectra_cljs.util :refer [get-first set-field!
                                       add-ids node-link]]
            [spectra_cljs.user :as user]
            [spectra_cljs.update :as u]
            [reagent.core :as r]
            [jayq.core :as jq])
  (:use [jayq.core :only [$]]))

;; TODO: Reorganize this by page
